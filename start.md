# 个人错题集管理系统 · 项目启动与部署说明

## 一、环境要求
- 已安装 **Node.js（建议 ≥ 14，本项目使用 22.x 验证通过）**，无需安装任何第三方 npm 依赖。
- 系统：Windows / macOS / Linux 均可。
- 数据库：**无**，全部数据以本地 JSON 文件存储于 `/data` 目录。

## 二、目录结构速览
```
wrongSet/
├── server.js              # Node 服务入口（一键启动）
├── public/                # 前端静态资源（原生 HTML/CSS/JS）
│   ├── img/uploads/       # 用户上传的图片落地目录（启动自动创建）
│   └── ...                # 8 个页面 + css/js
├── data/                  # JSON 持久化数据（自动初始化）
├── middleware/            # 中间件：鉴权 / 跨域 / 参数校验
├── tools/                 # 工具：文件锁 / 加密 / 文件读写 / 自增ID / multipart解析
│   ├── multipart.js       # 原生 multipart/form-data 解析器（零依赖）
│   └── env.js             # 极简 .env 读取器（零依赖）
├── route/                 # 接口路由层（user / error）
├── service/               # 业务逻辑层
│   └── imageService.js    # 图片上传业务：校验 / 落盘 / 物理删除
├── logs/                  # 运行日志 server.log
├── start.md               # 本文档
├── check-list.md          # 功能验收清单
└── boundary-note.md       # 边界场景处理说明
```

## 三、启动步骤
1. 打开终端，进入项目根目录：
   ```bash
   cd wrongSet
   ```
2. 启动服务（无需构建、无需安装依赖）：
   ```bash
   node server.js
   ```
   或使用指定端口：
   ```bash
   PORT=8080 node server.js
   ```
3. 看到如下输出即启动成功：
   ```
   ✅ 错题集管理系统已启动
      本地访问: http://localhost:3000
      仅允许 localhost 访问，数据存于 /data 目录
   ```

## 四、访问地址
| 页面 | 地址 | 说明 |
|------|------|------|
| 首页（公共错题） | http://localhost:3000/index.html | 所有人可访问 |
| 错题排行榜 | http://localhost:3000/rank.html | 所有人可访问 |
| 注册 | http://localhost:3000/register.html | 公开 |
| 登录 | http://localhost:3000/login.html | 公开 |
| 新增错题 | http://localhost:3000/add.html | 需登录 |
| 我的错题 | http://localhost:3000/myError.html | 需登录 |
| 错题分析报告 | http://localhost:3000/report.html | 需登录 |
| 错题详情 | http://localhost:3000/detail.html?errorId=1 | 公开题公开，私有题仅作者 |
| 个人设置 | http://localhost:3000/settings.html | 需登录 |
| 识别整卷错题（OCR） | http://localhost:3000/aiFind.html | 需登录 |

> ⚠️ 服务仅绑定 `127.0.0.1`（localhost），仅允许本地访问，跨域请求会被拦截。

## 五、首次使用流程
1. 访问 `/register.html` 注册账号（用户名限 4-16 位纯英文字母，注册后不可修改）。
2. 访问 `/login.html` 登录，服务端生成 Session（Cookie 形式，30 分钟无操作失效）。
3. 登录后进入「新增错题」添加题目，设为公开后首页/排行榜即可见。
4. 在首页/详情/排行榜对他人公开错题点赞、收藏。

## 六、数据说明
- 首次启动自动在 `/data` 生成 6 个空模板 JSON：`user.json / error.json / like.json / collect.json / session.json / autoId.json`。
- 若某 JSON 损坏，服务启动时会自动备份旧文件（`.bak.<时间戳>`）并重置为空模板，异常记录于 `logs/server.log`。
- 自增 ID（userId / errorId / likeId / collectId）由 `autoId.json` 维护，启动时会扫描现有数据防止错乱。

## 七、停止服务
- 终端 `Ctrl + C` 即可停止。
- 已登录会话在服务重启后会因过期清理而失效，需重新登录。

## 八、图片上传功能说明（新增）
- **范围**：仅用于「错题图片附件」，每个错题最多 **5 张**，单张 ≤ **2MB**，支持 **JPG / PNG / GIF / WEBP**。
- **使用入口**：
  1. 新增错题页（`add.html`）：选择图片后即时本地预览，提交时后端先落盘再随文本一起保存；
  2. 错题详情页（`detail.html`，仅作者可见）：可继续「添加图片」，或删除单张图片；
  3. 首页 / 我的错题 / 排行榜：卡片展示首图缩略图（多图显示张数角标）。
- **接口**：`POST /api/error/upload`（multipart/form-data，需登录）。字段 `images`（多文件）+ 可选 `errorId`（编辑时必带，用于校验归属与额度）。返回 `{ code:200, data:{ images:[相对URL] } }`。
- **存储**：图片落地于 `public/img/uploads/`，访问路径 `/img/uploads/xxx`。`error.json` 中错题新增 `images` 数组字段（仅本系统托管的相对路径，拒绝外部 URL，防 XSS / 路径穿越）。
- **清理**：删除错题、或编辑时移除某张图片，会同步删除对应磁盘文件，避免脏文件堆积。

## 八之二、详情页「显示/隐藏答案与解析」功能（新增）
- 错题详情页（`detail.html`）的「标准答案与解析」**默认整体隐藏**，仅展示「显示答案」按钮。
- 点击按钮**同时显示 / 隐藏「标准答案」与「解析」**，二者始终联动，不会出现「只显示答案不显示解析」或反之的情况；按钮文案在「显示答案 / 隐藏答案」之间变化。
- 答案与解析内容均已做 HTML 转义，隐藏态下也不会被意外渲染执行（防 XSS）。
- 该行为完全在前端 `public/js/detail.js` 中通过 `display` 切换实现，不依赖任何额外接口。

## 八之三、个人错题分析报告功能（新增）
- **入口**：导航栏「错题分析」（登录后可见），或直接访问 `/report.html`（未登录自动跳登录页）。
- **数据接口**：`GET /api/error/report`（需登录），后端聚合当前用户自己的错题（按 `ownErrorIds`），统计：
  - `total`：错题总数；
  - `byType`：按**错误类型**分布（降序，含数量）；
  - `byGrade`：按**年级**分布（降序，含数量）；
  - `bySubject`：按**学科**分布（降序，含数量）；
  - `suggestions`：`summary` 总评 + `list` 各类型针对性的**复习建议**（含占比）。
- **可视化（零第三方库，原生 Canvas 绘制）**：
  - **饼图（甜甜圈）**：直观展示各类错误类型占比，中心显示错题总数；
  - **水平条形图（直方图）**：展示各年级错题数量对比；
  - **水平条形图（直方图）**：展示各学科错题数量对比（与年级共用同一绘图函数）；
  - 饼图配套 HTML 图例（色块 + 类型名 + 数量 + 占比）。
- **复习建议**：根据占比最高的错误类型生成「优先突破」总评，并逐类型给出针对性复习方法（计算失误 / 概念混淆 / 审题错误 / 记忆遗漏 / 其他 各有专属文案）。
- **空状态**：尚未录入错题时，页面展示引导卡片并链接到「新增错题」。

## 八之四、学科（科目）字段与筛选功能（新增）
- **新增「学科」维度**：每道错题增加 `subject` 字段，枚举固定为 `语文 / 数学 / 英语 / 物理 / 化学 / 生物 / 政治 / 历史 / 地理 / 其他`，新增 / 编辑错题时**必选**，后端做枚举强校验（非法值返回 400）。
- **错题广场（`index.html`）**：顶部筛选条新增「学科类别」下拉，可按学科精确筛选公共错题；关键词搜索同时覆盖题目 / 类型 / 年级 / 学科。
- **我的错题（`myError.html`）**：新增「学科筛选」下拉，按本人学科快速过滤。
- **错题排行榜（`rank.html`）**：新增「选择学科榜单」下拉，按学科查看点赞排行。
- **详情页（`detail.html`）**：标题区展示学科标签，编辑弹窗可修改学科。
- **分析报告（`report.html`）**：概览新增「涉及学科」指标，并新增「各学科错题数量」条形图。
- **枚举下发**：`GET /api/meta` 新增返回 `subjects` 数组，前端下拉统一消费，与后端单一来源保持一致。

## 八之五、识别整卷错题功能（OCR 自动分析，已移除 AI 解答）
> 注：原「AI 解答题目」功能已按需求移除（删除 `aiSolve.html`、`public/js/aiSolve.js`、`route/aiRoute.js`、`service/aiService.js`、`.env` 及相关路由，不影响其余模块）。保留并改进的「识别错题」改为**浏览器端 OCR**，无需任何后端 AI、无需密钥。

- **入口**：导航栏「识别错题」（登录后可见），或直接访问 `/aiFind.html`。
- **原理**：前端引入 **Tesseract.js**（从 CDN 加载，免费、免密钥、运行时在浏览器内完成 OCR），将上传的试卷图片转为文字，再**用规则解析**批改标记：
  - **打叉（✗/×）/ 含「错、误、不正确」字样 / 出现「扣分、扣 N 分、减 N 分、失 N 分、-N 分」→ 判为错误**；
  - **打勾（✓/√）/ 含「对、正确、全对」→ 判为正确**；
  - 未识别到明确标记 → 判为「待确认」，由用户人工勾选。
- **去重规则（重点）**：一道题若**同时出现「打叉」和「扣分」等多种标记，只计为一次错误**（按题号分块合并，绝不重复列出同一题号）。跨页、OCR 重复出现的同一题号也会合并。
- **人工校正**：页面提供「补充说明」文本框，可填写如「第3题打了叉，第5题只扣了2分」，会一并参与识别判断，**显著提升准确率**；也可在结果区直接勾选 / 取消要加入错题集的题目。
- **转错题**：勾选的题目可「批量加入错题集」（或单题手动添加），跳转新增页并预填标题（第 X 题）与解析（OCR 判断依据），用户补全答案 / 类型 / 年级 / 学科即可。还提供「手动添加」输入框，直接录入题号加入。
- **降级与边界**：若浏览器离线导致 OCR 引擎（Tesseract.js）加载失败，页面自动切换为「手动添加」模式，不影响加入错题集；OCR 原文在结果区可展开核对。
- **免责**：页面顶部与结果区均显著标注「OCR 自动识别结果可能有误，仅供参考，请人工核对」。

## 八之六、识别错题的可靠性说明
- **OCR 能力边界**：Tesseract.js 对**印刷体**中文/英文识别较好，但对**手写批改符号（手画的叉、勾）识别较弱**——手画叉常被识别为字符 `×` 或噪声，手画勾可能丢失。因此本功能以「文字标记 + 扣分数字」为主要判断依据，对纯手写勾叉的试卷准确率有限。
- **提升准确率的方法**：① 上传清晰、正向、光线均匀的试卷照片；② 在「补充说明」里人工写出哪些题错了 / 扣了几分（强推荐）；③ 在结果区人工勾选确认。
- **若完全不满足需求**：可弃用此功能（仅删除 `public/aiFind.html`、`public/js/aiFind.js` 即可，不影响其余模块）。

## 九、常见问题
- **端口被占用**：用 `PORT=新端口 node server.js` 更换端口。
- **页面打不开**：确认服务已启动且通过 `http://localhost:端口` 访问（非 IP 或异地）。
- **接口 401**：会话已过期，请重新登录。
- **图片上传 400**：检查格式 / 大小 / 数量是否超限，或正在编辑的错题非本人。
- **识别错题页 OCR 失败 / 引擎加载失败**：本功能依赖浏览器从 CDN 加载 Tesseract.js（需联网）；离线时会自动切换为「手动添加」模式，可手动输入题号加入错题集。识别准确率有限，请务必人工核对。
- **图片不显示**：确认 `public/img/uploads/` 目录存在（启动自动创建），且文件名未被手动改动。
