# 工作总结 · 2026-07-09

> 项目：个人错题集管理系统（原生 Node.js + 前端本地 JSON 全栈，零第三方依赖）
> 工作目录：`/Users/hanks/Desktop/wrongSet`
> 服务地址：`http://localhost:3000`（已重置为空模板并运行）

---

## 一、今日核心工作概览

| 模块 | 内容 | 状态 |
| --- | --- | --- |
| 功能调整 | 放弃「AI 解答题目」，保留「识别错题」并改用 OCR 方案 | ✅ 完成 |
| 新增功能 | 「识别错题」：浏览器端 Tesseract.js OCR 识别整卷批改、规则解析错题、批量加入错题集 | ✅ 完成 |
| 全功能回归测试 | 编写并运行端到端 + 纯函数测试共 56 项 | ✅ 56/56 通过 |
| Bug 修复 | 修复 2 个测试中发现的功能性 Bug | ✅ 完成 |

---

## 二、识别错题功能（OCR 方案）

### 背景
用户原计划同时做「AI 解答题目」与「AI 寻找错题」两个 AI 功能。因免密钥免费视觉 API 实测不可靠（Pollinations 旧端点视觉退化、真视觉需 key），用户决定：**放弃 AI 解答，保留识别错题，改用 OCR 试试**。

### 实现方案
- **前端浏览器端 OCR**：引入 `tesseract.js@5`（CDN，免费、免密钥、无需后端），识别整卷图片文字。
- **规则解析**（纯函数 `aiFindParse.js`，UMD、环境无关、可在 Node 下单测）：
  - 打叉 ✗/×、含「错/误」、含「扣分/扣N分/减N分/-N分」 → 判为**错题**
  - 打勾 ✓/√、含「对/正确」 → 判为**正确**
  - 无标记 → **待确认**（人工核对）
  - **去重**：同题号（跨页/重复）合并，状态取最差（错 > 部分 > 对），「打叉 + 扣分」只算 1 次错误
- **离线降级**：OCR 引擎加载失败自动切换「手动添加」模式。
- **人机协同**：提供「补充说明」文本框，用户可手填 "第3题错、第5题扣2分" 提升准确率；结果区可展开 OCR 原文核对。

### 关键文件
- 新增/改造：`public/aiFind.html`、`public/js/aiFind.js`、`public/js/aiFindParse.js`
- 导航：原「AI 找错题」改名「识别错题」；移除「AI 解答」
- 已删除：`aiSolve.html`、`aiSolve.js`、`route/aiRoute.js`、`service/aiService.js`、`tools/env.js`、`.env`、`.env.example`

---

## 三、全功能回归测试

### 测试套件
| 套件 | 文件 | 项数 | 结果 |
| --- | --- | --- | --- |
| 端到端 API + 页面 | `/tmp/e2e_test.js` | 41 | ✅ 全部通过 |
| 识别错题解析纯函数 | `/tmp/aiFind_parse_test.js` | 15 | ✅ 全部通过 |
| **合计** | — | **56** | **✅ 全部通过** |

### 覆盖点
注册 / 登录 / 鉴权、meta 配置、错题增删改查、点赞收藏切换、图片上传落盘 + 删除级联清理、学科过滤、分析报告 bySubject、受保护页 302 跳转、识别错题页（含 tesseract 脚本）、已移除的 AI 解答（404）、静态资源、登出等。

---

## 四、测试中发现并修复的 2 个真实 Bug

### Bug 1 · 私密错题本人无法查看
- **现象**：`/api/error/detail` 等公开路由忽略会话 Cookie，导致默认私密（`isPublic=false`）的错题连本人访问都返回 403。
- **复现**：curl 链路 `add(200) → detail(带同一 cookie, 403)`；服务端日志显示 GET 请求未回填 userId。
- **修复**：公开路由改为「可选鉴权」——登录用户访问时回填 userId（本人看自己私密题 → 200），匿名访问他人私密题仍 403。
- **影响文件**：`server.js`

### Bug 2 · 上传图片不持久化
- **现象**：上传接口写入磁盘文件，却未把 URL 存回 `error.json.images` → 图片在详情页不显示，删除错题时也不清理（产生孤儿文件）。
- **复现**：`add → upload(200) → update(200) → detail(图片缺失)`；`error.json` 中 images 始终为空数组。
- **修复**：新增 `errorService.addImages()`（校验归属 + 5 张上限），上传后落库；`deleteError` 即可级联清理磁盘图片。
- **影响文件**：`service/errorService.js`、`route/errorRoute.js`

---

## 五、测试过程中澄清的非 Bug 项（避免误判）

| 现象 | 结论 |
| --- | --- |
| 登录错误密码返回 400（非 401） | 应用设计选择，功能正确；如需更规范可改 401 |
| 点赞/收藏重复点击返回取消（非 409） | toggle 语义（再点=取消），非去重报错 |
| 新增私密错题不出现在广场列表 | 广场仅展示公开题，正确隐藏 |
| 上传返回 URL 已含 `/img/uploads/` 前缀 | 前端拼接路径时勿重复前缀 |

> 注：上述现象均为测试脚本预期错误或应用设计，非功能缺陷。

---

## 六、已知局限与后续建议

1. **OCR 对手写批改符号（手画叉/勾）识别较弱**，主要靠「文字标记 + 扣分数字」判断。建议用户善用「补充说明」人工校正，并在结果区人工勾选核对。
2. **OCR 引擎从 CDN 加载，需联网**；离线时自动降级「手动添加」。
3. 若「识别错题」功能完全不满足需求，按用户「可放弃」约定，仅删除 `aiFind.html` + `aiFind.js` + `aiFindParse.js` 即可，不影响其他模块。
4. 登录错误密码可考虑改为标准 401 响应码（当前 400）。

---

## 七、当日代码与文档产出清单

**新增/改造文件**
- `public/aiFind.html`、`public/js/aiFind.js`、`public/js/aiFindParse.js`
- `server.js`（公开路由可选鉴权）、`service/errorService.js`（addImages）、`route/errorRoute.js`（上传落库）
- `public/js/common.js`、`public/js/api.js`、`public/css/global.css`（导航改名、样式）

**删除文件**
- `public/aiSolve.html`、`public/js/aiSolve.js`、`route/aiRoute.js`、`service/aiService.js`、`tools/env.js`、`.env`、`.env.example`

**文档**
- `start.md`、`check-list.md`、`boundary-note.md`（同步更新为 OCR 口径）
- `测试报告-2026-07-09.md`（详细测试报告）
- 本文件 `工作总结-2026-07-09.md`

**测试脚本（可复跑）**
- `/tmp/e2e_test.js`、`/tmp/aiFind_parse_test.js`

---

*— 总结完毕，服务运行于 http://localhost:3000，数据已重置为干净空模板。*
