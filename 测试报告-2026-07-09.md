# 错题集管理系统 — 功能测试报告（2026-07-09）

> 本次目标：对 `wrongSet` 个人错题集管理系统进行**全功能回归测试**，覆盖注册/登录、错题增删改查、点赞/收藏、图片上传、学科维度、分析报告、识别错题（OCR）页面与已移除的 AI 解答功能；并在测试中发现并修复了 2 个真实功能 Bug。

## 一、测试环境

| 项目 | 说明 |
| --- | --- |
| 运行目录 | `/Users/hanks/Desktop/wrongSet` |
| 运行时 | Node.js 22.22.2（受管版本，零第三方依赖） |
| 启动方式 | `node server.js`（监听 `http://localhost:3000`） |
| 数据 | 每次测试前 `rm -f data/*.json` 重置为干净空模板，测试后再次重置并重启服务 |
| 测试脚本 | `/tmp/e2e_test.js`（端到端，41 项）、`/tmp/aiFind_parse_test.js`（解析纯函数，15 项） |

## 二、测试结果总览

| 测试套件 | 项数 | 结果 |
| --- | --- | --- |
| 端到端 API + 页面（e2e_test.js） | 41 | ✅ 41 / 41 通过 |
| 识别错题解析纯函数（aiFind_parse_test.js） | 15 | ✅ 15 / 15 通过 |
| **合计** | **56** | **✅ 全部通过** |

## 三、测试覆盖范围（41 项 e2e 明细）

| 分组 | 验证点 |
| --- | --- |
| 1. 注册/登录/鉴权 | 注册成功 200、重复注册 409、用户名过短 400、错误密码 400、正确登录 200、sid Cookie 下发 |
| 2. /api/meta | 返回年级/类型/学科枚举，含「数学」 |
| 3. 新增错题（带学科） | 成功 200、subject 正确落库 |
| 4. 学科枚举强校验 | 非法学科 400 |
| 5. 广场列表 + 学科过滤 | 按学科过滤命中、关键字「二次函数」命中 |
| 6. 详情 | 返回全量含 subject |
| 7. 点赞 | 点赞成功(liked=true)、再次点击=取消(去重切换) |
| 8. 收藏 | 收藏成功(collected=true)、再次点击=取消(去重切换) |
| 9. 我的错题 | 含新增记录 |
| 10. 排行榜（按学科） | 学科榜单返回数组 |
| 11. 分析报告 | 返回 byType/byGrade/**bySubject**、统计到「数学 1 题」 |
| 12. 图片上传 | 单图上传 200、文件落盘到 `public/img/uploads/` |
| 13. 编辑/删除 | 编辑成功、标题/学科更新、删除成功、删除后详情 400、删除级联清理磁盘图片 |
| 14. 受保护页面鉴权 | 未登录访问 aiFind/add → 302 跳转登录；已登录 report → 200 |
| 15. 识别错题页 | 已登录 aiFind.html → 200 且含 `tesseract.min.js` 与解析脚本 |
| 16. 已移除的 AI 解答 | aiSolve.html → 404、`/api/ai/solve` → 404、`/api/ai/find` → 404 |
| 17. 静态资源 | index.html / aiFindParse.js / global.css 均可访问 |
| 18. 登出 | 登出成功、登出后 my 接口 401 |

解析纯函数 15 项覆盖：打叉+扣分同题号只计 1 次错误、跨页同题号合并、无标记→待确认、同题多「第X题」拆分、中文数字题号、得分提取、仅扣分→wrong 等。

## 四、测试中发现并修复的 Bug（重点）

### Bug 1：私密错题本人无法查看（影响核心可用性）
- **现象**：默认 `isPublic=false`（私密）的错题，即使本人已登录，打开详情页也返回 `403 无权查看该私有错题`。
- **根因**：`server.js` 中 `/api/error/detail` 等路由标记为 `auth:false`，服务端**完全忽略请求中的会话 Cookie**，`getDetail` 拿到的 `userId` 恒为 `null`，导致私密题对所有人（含本人）一律隐藏。
- **修复**：公开路由改为「可选鉴权」——有合法会话则回填 `userId`（本人可看自己的私密题），无会话则为 `null`（私密题仍对他人隐藏）。匿名访问他人私密题依旧 403，公开题仍可看。
- **验证**：修复后本人带 Cookie 查看自己私密题 → 200；匿名查看 → 403。

### Bug 2：上传图片不持久化（导致图片不显示 + 删除不清理）
- **现象**：上传图片接口返回成功并给出 URL，但图片既不显示在详情页，删除错题时磁盘文件也不被清理（产生孤儿文件）。
- **根因**：`errorRoute.upload` 调用 `imageService.uploadImages` 写入文件并返回 URL，但**从未把 URL 写回 `error.json` 的 `images` 字段**；删除时 `removedImages` 为空，故不触发磁盘清理。
- **修复**：新增 `errorService.addImages()`（校验归属 + 5 张上限），上传路由在落盘后将该批 URL 持久化到错题；删除时即可级联清理。
- **验证**：上传后图片写入 `error.json.images`；删除错题后磁盘文件被移除（e2e 第 12、13 组通过）。

## 五、测试过程中澄清的「非 Bug」项（避免误判）

| 现象 | 结论 |
| --- | --- |
| 登录错误密码返回 400 而非 401 | 属应用设计选择（统一用 400 表示凭证错误），功能正确；建议后续可改为更规范的 401 |
| 私错题不出现在广场列表 | 正确行为：广场（`/api/error/list`）仅展示 `isPublic=true` 的题 |
| 详情接口参数名为 `errorId` | 前端 `api.js:54` 正确发送 `?errorId=`；测试脚本曾误用 `?id=` 导致 400，已更正 |
| 点赞/收藏为「切换」语义 | 重复点击为取消（非 409 去重报错），符合 toggle 设计 |

## 六、如何复跑测试

```bash
cd /Users/hanks/Desktop/wrongSet
# 启动服务（重置数据）
rm -f data/*.json
node server.js &           # 监听 3000

# 端到端测试（另开终端）
node /tmp/e2e_test.js      # 期望：通过 41 / 失败 0

# 解析纯函数单测
node /tmp/aiFind_parse_test.js   # 期望：15 通过 / 0 失败
```

## 七、遗留说明

- **识别错题（OCR）** 依赖浏览器端从 CDN 加载 `tesseract.js@5`，需联网；离线时页面自动降级为「手动添加」模式。OCR 对**手写批改符号**（手画叉/勾）识别较弱，主要靠「文字标记 + 扣分数字」判断，建议在「补充说明」人工校正并人工勾选核对。
- 服务当前已重置为干净空模板并运行于 `http://localhost:3000`。
